{
  "name": "gcloud-node-datastore-utils",
  "version": "0.0.4",
  "description": "Utils method to create, update, read & delete gcloud datastore entities",
  "repository": {
    "type": "git",
    "url": "https://github.com/fashionm/gcloud-datastore-utils.git"
  },
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha --compilers js:babel-core/register"
  },
  "main": "index.js",
  "dependencies": {
    "babel-polyfill": "^6.7.4"
  },
  "devDependencies": {
    "babel-cli": "^6.5.4",
    "babel-eslint": "^6.0.0",
    "babel-preset-es2015-node5": "^1.2.0",
    "babel-preset-stage-0": "^6.5.0",
    "eslint": "^2.5.3",
    "eslint-config-airbnb": "^6.2.0"
  }
}
